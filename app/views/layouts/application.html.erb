<!DOCTYPE html>
<html>
  <head>
    <title>UrlEncoder</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <script src="https://unpkg.com/@popperjs/core@2"></script>
    <script src="https://unpkg.com/tippy.js@6"></script>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <main class="prose mx-auto mt-28 px-5">
      <%= tag.p alert, class:"bg-yellow-100 text-yellow-800 px-2 rounded" %>
      <%= tag.p notice, class:"bg-blue-100 text-blue-800 px-2 rounded" %>
      <nav class="flex items-center justify-between mb-4">
        <h1 class="px-1 m-0"><%= link_to "URL Encoder", root_path, class:'font-black no-underline' %></h1>
        <div class="flex items-center gap-3">
          <% if user_signed_in? %>
            <%= link_to "Profile", edit_user_registration_path %>
            <%= button_to "Logout", destroy_user_session_path, method: :delete, class: "cursor-pointer underline" %>
          <% else %>
            <%= link_to "Login", new_user_session_path %>
            <%= link_to "Sign Up", new_user_registration_path %>
          <% end %>
        </div>
      </nav>
      <%= yield %>
    </main>
  </body>
</html>
